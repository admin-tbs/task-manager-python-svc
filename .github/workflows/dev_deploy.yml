name: Task Manager Pipeline

on: 
  push:
    branches:
      - task_manager_v1
  pull_request:
    branches:
      - task_manager_v1

jobs:
  task-manager-deployment:
    runs-on: ubuntu-latest

    steps:
      - name: CheckOut Code
        uses: actions/checkout@v4

      - name: Setup-Python
        run: |
          echo "python-version:'3.12'"

      - name: Install Requirement
        run: |
          echo "pip install -r requirement.txt"

      - name: Deployment
        run: |
          echo "Started the Deployment"
          echo "Deployment In Progress"
          echo "Deployment Completed"

      
